# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Purpose

This repository is intended to convert dbt documentation into PDF format. The project is currently empty — no source files, dependencies, or tooling have been added yet.

When the project is bootstrapped, update this file with:
- How to install dependencies and run the tool
- How to run tests (and a single test)
- How to lint/format
- The high-level architecture (e.g., how dbt docs are fetched/parsed and rendered to PDF)

## ゴール
dbt docs generateで生成されるメタ情報を活用してある程度きれいなpdf形式のテーブルカタログを作成すること
pandasは利用せずpipで導入可能なツールのみを活用して
